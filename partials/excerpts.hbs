{{#foreach posts}}

    <div class="sample animated fadeIn">
        <div class="container">
            <div class="title animated fadeInUp">
                <a href="{{url}}">
                    <h1>{{title}}</h1>
                </a>
            </div>
            {{> sidebar}}
        </div>
    </div>

    <article class="animated fadeIn {{post_class}}">
        <div class="container">
            <p>{{excerpt words="100"}}<a href="{{url}}">阅读更多&hellip;</a></p>
        </div>
    </article>

{{/foreach}}
