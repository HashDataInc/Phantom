{{#foreach posts}}

    <div class="sample animated fadeIn">
        <div class="container">
            <div class="title animated fadeInUp">
                <a href="{{url}}">
                    <h1>{{title}}</h1>
                </a>
            </div>
            {{> sidebar}}
        </div>
    </div>

    <article class="animated fadeIn {{post_class}}">
        <div class="container">
            <p>{{excerpt characters="150"}}&hellip;<a href="{{url}}">阅读全文</a></p>
        </div>
    </article>

{{/foreach}}
